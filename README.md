# EKS CDK with FARGATE

This is a sample project to create EKS cluster with Fargate profile and deploy nginx service with LoadBalancer with Python CDK.


```
$ python3 -m venv .venv
```

```
$ pip install -r requirements.txt
```

```
$ cdk synth && cdk deploy
```
